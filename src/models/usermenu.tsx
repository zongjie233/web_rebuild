import React, { useState } from "react";
import { createModel } from "hox";

const setupTableData = (treeData: []) => {
  const tableData: [] = [];
  let index = 0;
  const parseTreeData = () => {};
};

function useMenu() {
  const [treeData, setPeerTreeData] = useState([]);
  const [tableData, setTableData] = useState([]);
}

export default createModel(useMenu);
